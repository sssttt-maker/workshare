<%= link_to '新規作成', new_room_path, class: 'btn btn-primary' %>


<div>
  <ul>
    <% @rooms.each do |room| %>
      <li><%= link_to "ROOM#{room.id}", room_path(room) %></li>
    <% end %>
  </ul>
</div>
